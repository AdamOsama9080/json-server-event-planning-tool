{
  "name": "json-server-event-planning-tool",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch events.json --port 5000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.1"
  }
}
